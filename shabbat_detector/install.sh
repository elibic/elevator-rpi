#!/usr/bin/env bash
# ──────────────────────────────────────────────────────────────────────────────
# DEPRECATED — מוחלף ע"י setup.sh בשורש הריפו.
# נשאר כ-wrapper תאימות-לאחור: מפנה ל-setup.sh שמתקין את *כל* המערכת
# (דרייברים, שני השירותים, הגדרות, התראות, קיצור דרך) בסדר הנכון.
#
#   ההמלצה:  sudo ./setup.sh           (טרמינל)   |   sudo ./setup.sh --web (גרפי)
# ──────────────────────────────────────────────────────────────────────────────
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
echo "shabbat_detector/install.sh הוחלף ע\"י setup.sh — מריץ אותו…" >&2
exec "$ROOT/setup.sh" "$@"
