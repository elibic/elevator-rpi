# =========================================================
#  .gitignore — מה לא להעלות ל-GitHub
# =========================================================

# ⛔ קונפיג סודי / לכל מכשיר — לעולם לא מעלים!
#    (מכיל SECRET_KEY ומיפוי תגיות. השתמש ב-rfid_config.example.json כתבנית)
rfid_config.json

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# לוגים, גיבויים, ארכיונים
*.log
*.zip
*-backups/
